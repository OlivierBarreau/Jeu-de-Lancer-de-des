{% extends 'base.html.twig' %}
{% block title 'Dice game' %}

{% block body %}
    <div class="body-content">

        <h2> Bienvenue </h2>
        <p> Remplissez ce formulaire pour définir les paramètres du jeu </p>
        
        <div id="incriptionForm" class="form">
            {{ form_start(form) }}
                <div class="formRow">{{ form_row(form.pseudoPremierJoueur) }}</div>

                <div class="formRow">{{ form_row(form.pseudoDeuxiemeJoueur) }}</div>

                <div class="form-group">
                    <button class="btn btn-primary"> Lancer le jeu </button>
                </div>
            {{ form_end(form) }}
        </div>

    </div>
{% endblock %}